program zad3;
const n=20;
var i,b,s:integer;
a:array[-52..65] of integer;
var max, nmax, min, nmin,z:real;
begin
  for i:=1 to n do
    begin
    a[i]:= -50 +random(100);
  write(a[i]:4);
  end;
  max:=a[i]; nmax:=1;
for i:=0 to n do
  if (a[i]<0) and (a[i]>max)  then

begin
max:=a[i];
nmax:=i;
end;
write(' max=',max,' nmax=',nmax);
begin
  for i:=0 to n do
    if i<min then 
      end;
    
    writeln('m',a[i]);
      end.
      
